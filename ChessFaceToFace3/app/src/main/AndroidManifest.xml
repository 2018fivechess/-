<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.junruitian.chessfacetoface">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FirstActivity"
            android:label="AICHESS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:label="请选择棋类种类：" />
        <activity
            android:name=".ThirdActivity"
            android:label="请选择游戏模式：" />
        <activity android:name="com.example.junruitian.Logaction.LogActivity"
            android:label="请输入邮箱和密码进行登陆"
            ></activity>
    </application>

</manifest>